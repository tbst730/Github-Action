site_name: actions_workshop
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: actions_workshop
      handlers:
        python:
          options:
            docstring_style: numpy